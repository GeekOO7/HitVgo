# 修改记录

## 2026-08-22

### 打开页中央预览

- 加载项目后隐藏「首帧生成」操作区，中央改为视频播放器且不自动播放。
- 时间标尺当前位置有成片时，暂停并显示对应帧；无片源时为黑屏，不再显示「中央预览」提示文案。
- 暂停在 0 秒时略微 seek，避免浏览器画不出首帧。

### 生成首帧图入口

- 左侧栏「去生成」改为「生成首帧图」，样式与「剧情分镜工具」按钮一致（高亮）。
- 点击后才展开中央首帧生成区。

### 时间轴

- 工具栏增加「在播放头断开」按钮（✂），快捷提示含 `S`。
